<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Capivis App" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Add Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Navigation Bar -->

    <!-- Landing Page Section -->
    <div class="container d-flex justify-content-center align-items-center" style="height: 100vh;">
      <div class="text-center">
        <h1 class="display-3">Welcome to Capivis App!</h1>
        <p class="lead">Your journey starts here.</p>

        <!-- Donor Buttons Below the Welcome Message -->
        <% if user_signed_in? && current_user.role == 'donor' %>
          <% if current_user.donor.present? %>
            <%= link_to "View My Donor Profile", donors_path, class: "btn btn-primary mt-3" %>
          <% else %>
            <%= link_to "Register Now", new_donor_path, class: "btn btn-primary mt-3" %>
          <% end %>
        <% end %>
        
      </div>
    </div>
    
    <%= yield %>

    <!-- Add Bootstrap JS and dependencies -->
  </body>
</html>
